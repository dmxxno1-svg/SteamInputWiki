<<<<<<< Updated upstream
# 02b02：按键面板（Button Pad）

推荐观看：Critical Composers 相关视频（链接待补）

该输入风格以四个面部按键组成的菱形阵列为表现形式，类似现代手柄上的 ABXY（Xbox）、BAYX（Nintendo）或十字/圆/方/三角（Sony）。因此其工作方式与上述面部按键簇相同，提供四个绑定槽位。

该风格可分配给：

* 实体面部按键簇
=======
```markdown
# 02b02：按键面板（Button Pad）

推荐观看：Critical Composers 的相关视频

该输入样式模拟面部按键簇或四个按键的菱形布局，等同于现代手柄的 ABXY（Xbox）、BAYX（Nintendo）或索尼的十字/圆/方/三角。它提供 4 个绑定槽位，每个按键一个。

可将此样式分配给：

* 真正的面部按键组
>>>>>>> Stashed changes
* 方向键（D-Pads）
* 触控板
* 摇杆

<<<<<<< Updated upstream
当分配给实体面部按键簇时，其行为完全如预期。

当分配给方向键时，其行为大致相同，但受限于方向键的物理限制（无法同时按下相反方向，因此无法实现某些组合如 X+B 或 A+Y 等）。

当分配给 xy 坐标设备（如触控板和摇杆）时，会在 xy 坐标平面上划分四个区域，对应按键绑定并在设备输出该坐标时触发。SOCD 在这种情况下不可发生，且根据设置，某些按键组合（例如 A+B、B+Y、A+X、X+Y）也可能无法触发。

对实体面部按键簇与方向键而言，设置非常有限：你只能更改绑定并设置模式切换（Mode Shift）。

对 xy 坐标设备，则有另外三个设置：
=======
当分配给实际的面部按键组时，行为与常规手柄按键一致。

当分配给 D-Pad 时，基本上也与面部按键类似，但受限于 D-Pad 的物理特性（不能同时触发对立方向，因此无法形成某些组合键）。

当分配给具有 xy 坐标的设备（触控板与摇杆）时，会在坐标平面上创建 4 个区域，对应 4 个按键并在坐标输出落入对应区域时触发。此类设备无法发生 SOCD（同时相反方向），并且某些按键组合在设置上也可能不可用。

对于实际面部按键与 D-Pad，设置较为有限：可以更改绑定并设置模式切换（Mode Shift）。

对于 xy 设备，有另外三个设置：
>>>>>>> Stashed changes

## 设置

### 绑定（Bindings）

<<<<<<< Updated upstream
该模式的默认绑定为 Xbox 的 ABXY 按键，位置符合常规预期。但你可以将绑定改为任何你想要的内容，例如在仅用键盘与鼠标的游戏中做对应映射。

### 模式切换（Mode Shifting）

允许你设置一个按键，将该输入设备切换为完全不同的输入风格。模式切换在第 04 章有详细介绍。

### 按键半径（Button Radius）

仅适用于 xy 坐标设备。

此滑块控制按键区域的大小（即每个虚拟按键的半径）。

### 按键间距（Button Distance）

仅适用于 xy 坐标设备。

此滑块控制按键区域之间的距离，以及 xy 坐标的中心位置。

### 半径与间距共同作用

通过调整这两个滑块，你可以配置虚拟按键之间无重叠的排列，或让按键区域按需重叠。

### 需要点击（Requires Click）

仅适用于 xy 坐标设备。

这是一个开/关设置，用于决定是否需要按下设备（例如触控板按压）才能发送按键。
=======
默认绑定为 Xbox 的 ABXY，按常规位置排列。可以按需更改为其他绑定，例如在仅键鼠游戏中使用键盘按键。

### 模式切换（Mode Shifting）

允许把某个按键设为切换当前输入样式到完全不同的输入样式。模式切换在第 4 章有更详细说明。

### 按键半径（Button Radius）

仅对 xy 设备可用。此滑块决定每个虚拟按键区域的大小。

### 按键间距（Button Distance）

仅对 xy 设备可用。此滑块决定按键区域之间的间距以及 xy 坐标的中心点。

这两个滑块共同作用，可以配置虚拟按键是否相互重叠以及重叠程度。

### 需要点击（Requires Click）

仅对 xy 设备可用。二值设置，决定是否需要物理点击才能发送按键触发。

```
>>>>>>> Stashed changes
